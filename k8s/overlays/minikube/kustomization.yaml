apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fraud-detection

resources:
  - ../../base

patches:
  - target:
      kind: Service
      name: fraud-detection-api-lb
    patch: |-
      - op: replace
        path: /spec/type
        value: NodePort